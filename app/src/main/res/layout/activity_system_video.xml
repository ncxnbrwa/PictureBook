<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/root_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_picture_book"
    tools:context=".SystemVideoActivity">

    <com.example.picturebook.view.CustomVideoView
        android:id="@+id/video_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:layout_marginLeft="8dp"
        android:layout_marginTop="30dp"
        android:layout_marginRight="8dp"
        android:layout_marginBottom="60dp"
        android:orientation="horizontal">

        <ProgressBar
            android:id="@+id/progress"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="0dp"
            android:layout_height="14dp"
            android:layout_weight="7"
            android:max="100"
            android:progress="0"
            android:progressDrawable="@drawable/update_progress" />

        <TextView
            android:id="@+id/tv_video_time"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            tools:text="60"
            android:textColor="#333333"
            android:textSize="16sp" />
    </LinearLayout>

    <ImageButton
        android:id="@+id/btn_back"
        android:layout_gravity="left|bottom"
        style="@style/ActionButtonStyle"
        android:src="@drawable/icon_back" />

    <ImageButton
        android:id="@+id/btn_pre"
        style="@style/ActionButtonStyle"
        android:layout_gravity="right|bottom"
        android:src="@drawable/icon_pre" />

    <include layout="@layout/choice_question_layout" />
</FrameLayout>